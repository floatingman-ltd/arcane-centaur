== Configuration Architecture

This document explains the directory structure of this Neovim
configuration — what each directory and file is for, why it is placed
where it is, and how the pieces fit together at startup.

*Quick navigation*

[cols="1,3"]
|===
| xref:#supported-languages[Supported Languages] | Language support matrix
| xref:#plugin-overview[Plugin Overview] | All plugins grouped by category
| xref:#startup-order[Startup Order] | Init file loading sequence
| xref:#root-level-files[Root-Level Files] | init.lua, lazy-lock.json
| xref:#lua[lua/] | options, keymaps, plugins, config modules
| xref:#after-ftplugin[after/ftplugin/] | Filetype-specific overrides
| xref:#docker[docker/] | Docker service definitions
| xref:#docs[docs/] | User documentation
| xref:#openspec[openspec/] | Change management workflow
| xref:#github[.github/] | GitHub Actions (docs CI)
| xref:#claude[.claude/] | Claude Code skills & settings
| xref:#console-detection[Console Detection] | How is_console is determined
| xref:#open-url[Open URL Behaviour] | Browser vs. notification
|===

'''''

[[supported-languages]]
== Supported Languages

[width="100%",cols="20%,15%,20%,20%,15%,10%",options="header",]
|===
|Language |Treesitter |LSP |REPL |Structural Editing |Guide

|Clojure
|✅
|—
|✅ nREPL (Conjure)
|✅ vim-sexp {plus} parinfer
|xref:languages/lisp.adoc[Lisp guide]

|Common Lisp
|✅
|—
|✅ Swank (Conjure)
|✅ vim-sexp {plus} parinfer
|xref:languages/lisp.adoc[Lisp guide]

|C++#++
|✅
|✅ roslyn.nvim
|—
|—
|xref:languages/dotnet.adoc[.NET guide]

|F++#++
|✅
|✅ fsautocomplete
|✅ dotnet fsi (iron.nvim)
|—
|xref:languages/dotnet.adoc[.NET guide]

|Fennel
|—
|—
|✅ (Conjure)
|✅ parinfer
|xref:languages/lisp.adoc[Lisp guide]

|Haskell
|—
|✅ haskell-tools
|✅ GHCi
|—
|xref:languages/haskell.adoc[Haskell guide]

|HTTP
|✅
|—
|—
|—
|xref:content/rest.adoc[REST guide]

|Janet
|—
|✅ janet_lsp
|✅ (Conjure)
|✅ vim-sexp {plus} parinfer
|xref:languages/janet.adoc[Janet guide]

|Lua
|✅
|—
|—
|—
|—

|Markdown
|✅
|✅ marksman
|—
|—
|xref:content/markdown.adoc[Markdown guide]

|Mermaid _(in Markdown)_
|✅ _(markdown)_
|—
|—
|—
|xref:content/diagrams.adoc[Diagrams guide]

|MARP slides
|✅ _(markdown)_
|—
|—
|—
|xref:content/presentations.adoc[Presentations guide]

|PlantUML
|✅
|—
|—
|—
|xref:content/diagrams.adoc[Diagrams guide]

|Scheme
|✅
|—
|✅ built-in (Conjure)
|✅ vim-sexp {plus} parinfer
|xref:languages/lisp.adoc[Lisp guide]
|===

'''''

[[plugin-overview]]
== Plugin Overview

All plugins are managed by https://github.com/folke/lazy.nvim[lazy.nvim]. Specs live in `lua/plugins/` — one file per group.

=== Colorscheme

[cols="1,3"]
|===
|https://github.com/folke/tokyonight.nvim[tokyonight.nvim] |TokyoNight color scheme (moon variant by default)
|===

=== Completion & LSP

[cols="1,3"]
|===
|https://github.com/neovim/nvim-lspconfig[nvim-lspconfig] |LSP server configuration helpers
|https://github.com/saghen/blink.cmp[blink.cmp] v1 |Completion engine (Rust fuzzy matcher, built-in LSP/buffer/path/snippet sources). Replaces nvim-cmp.
|https://github.com/saghen/blink.compat[blink.compat] |Bridges legacy nvim-cmp sources (cmp-spell, cmp-conjure) into blink.cmp.
|https://github.com/f3fora/cmp-spell[cmp-spell] |Spell dictionary source (bridged via blink.compat; active only when `:set spell`)
|https://github.com/stevearc/conform.nvim[conform.nvim] |Format-on-save and manual formatting
|===

=== Navigation & UI

[cols="1,3"]
|===
|https://github.com/ibhagwan/fzf-lua[fzf-lua] |Fuzzy finder for files, grep, buffers, diagnostics
|https://github.com/nvim-tree/nvim-tree.lua[nvim-tree.lua] |File explorer sidebar
|https://github.com/nvim-lualine/lualine.nvim[lualine.nvim] |Status line (tokyonight theme, globalstatus, gitsigns diff + diagnostics)
|https://github.com/nvim-tree/nvim-web-devicons[nvim-web-devicons] |File type icons (requires Nerd Font)
|https://github.com/folke/trouble.nvim[trouble.nvim] v3 |Structured panel for diagnostics, symbols, LSP references, quickfix, and loclist (`<leader>x` group)
|https://github.com/folke/todo-comments.nvim[todo-comments.nvim] |Highlight and list `TODO`/`FIXME`/`HACK`/`NOTE`/`WARN` annotations; integrates with trouble and fzf-lua
|===

=== Editor Enhancement

[cols="1,3"]
|===
|https://github.com/nvim-treesitter/nvim-treesitter[nvim-treesitter] (pinned: `master`) |Syntax highlighting and indent. Pinned to `master` and configured via `require("nvim-treesitter.configs").setup(opts)` so the `opts`-style API (`ensure_installed`/`highlight`/`indent`) is active. (Text objects were removed — the `master` query path is broken on Neovim 0.12.)
|https://github.com/kylechui/nvim-surround[nvim-surround] |Surround text with quotes, brackets, tags (`ys`/`cs`/`ds`, identical keys to vim-surround)
|https://github.com/tpope/vim-unimpaired[vim-unimpaired] |Paired bracket keymaps (`[b`, `]b`, etc.)
|https://github.com/tpope/vim-repeat[vim-repeat] |Repeat plugin commands with `.`
|https://github.com/jakewvincent/mkdnflow.nvim[mkdnflow.nvim] |Markdown link following and note navigation
|===

=== Git

[cols="1,3"]
|===
|https://github.com/tpope/vim-fugitive[vim-fugitive] |Git commands inside Neovim (`:G`, `:Gdiff`, etc.)
|https://github.com/lewis6991/gitsigns.nvim[gitsigns.nvim] |Inline hunk signs, stage/unstage hunks
|https://github.com/sindrets/diffview.nvim[diffview.nvim] |Side-by-side diff and file history views
|===

=== Languages — Lisp Family

[cols="1,3"]
|===
|https://github.com/Olical/conjure[conjure] |Evaluate code in a running REPL from the editor
|https://github.com/PaterJason/cmp-conjure[cmp-conjure] |Conjure completion source (bridged into blink.cmp via blink.compat)
|https://github.com/guns/vim-sexp[vim-sexp] |Structural S-expression editing
|https://github.com/tpope/vim-sexp-mappings-for-regular-people[vim-sexp-mappings-for-regular-people] |Friendlier default keymaps for vim-sexp
|https://github.com/gpanders/nvim-parinfer[nvim-parinfer] |Automatic parenthesis balancing via indentation
|https://github.com/HiPhish/rainbow-delimiters.nvim[rainbow-delimiters.nvim] |Color-coded nested delimiters
|===

=== Languages — .NET

[cols="1,3"]
|===
|https://github.com/seblyng/roslyn.nvim[roslyn.nvim] |Roslyn LSP for C++#++ (OmniSharp successor)
|https://github.com/Vigemus/iron.nvim[iron.nvim] |REPL management (used for F++#++ `dotnet fsi`)
|===

=== Languages — Haskell

[cols="1,3"]
|===
|https://github.com/mrcjkb/haskell-tools.nvim[haskell-tools.nvim] |Haskell LSP, GHCi REPL, Hoogle search
|===

=== AI & Automation

[cols="1,3"]
|===
|https://github.com/yetone/avante.nvim[avante.nvim] v0.1.* |AI chat panel, Ollama-only backend (Claude disabled — Anthropic subscription-OAuth ToS). dressing.nvim removed (archived upstream); vim.ui falls back to native.
|https://github.com/MunifTanjim/nui.nvim[nui.nvim] |UI component library (required by avante)
|https://github.com/nvim-lua/plenary.nvim[plenary.nvim] |Lua utility library (required by avante + diffview)
|===

=== Content & Preview

[cols="1,3"]
|===
|https://github.com/habamax/vim-asciidoctor[vim-asciidoctor] |AsciiDoc syntax, section folding, and fenced-code-block highlighting under the canonical `asciidoctor` filetype (`*.adoc`/`*.asciidoc` → `asciidoctor` via `vim.filetype.add`).
|https://github.com/OXY2DEV/markview.nvim[markview.nvim] |_(deferred)_ In-buffer AsciiDoc rendering requires the `cathaysia/tree-sitter-asciidoc` treesitter grammar, which is not in nvim-treesitter master. Re-enable when the grammar stabilises.
|https://github.com/iamcco/markdown-preview.nvim[markdown-preview.nvim] |Live Markdown preview in browser (GUI)
|https://github.com/ellisonleao/glow.nvim[glow.nvim] |Terminal Markdown preview via glow (console)
|https://github.com/aklt/plantuml-syntax[plantuml-syntax] |PlantUML syntax highlighting
|https://github.com/mistweaverco/kulala.nvim[kulala.nvim] |HTTP/REST client for `.http` files
|https://github.com/turbio/bracey.vim[bracey.vim] |Live HTML preview in browser
|===

'''''

[[startup-order]]
== Startup Order

Neovim always loads files in this sequence:

....
init.lua
  └── lua/options.lua      (vim options and leader keys)
  └── lua/loader/init.lua  (bootstraps lazy.nvim; loads all plugins)
  └── lua/keymaps.lua      (global keymaps)

after/ftplugin/<ft>.lua    (loaded by Neovim automatically when a filetype buffer opens)
....

Everything else is loaded on-demand — either by lazy.nvim when a plugin
is first needed, or by `require()` calls inside plugin config functions.

'''''

[[root-level-files]]
== Root-Level Files

[width="100%",cols="50%,50%",options="header",]
|===
|File |Purpose
|`init.lua` |Entry point. Requires `options`, `loader`, and `keymaps` in
order. Nothing else belongs here.

|`lazy-lock.json` |Pinned commit hashes for every plugin. Guarantees
reproducible installs. Managed by lazy.nvim — commit this file so all
machines stay in sync.

|`readme.md` |Human-readable overview: keybindings, supported languages,
plugin list, Docker services.

|`CLAUDE.md` |Project guidance for Claude Code (architecture, conventions,
commands).
|===

'''''

[[lua]]
== `lua/` — The Lua Source Tree

All runtime Lua code lives here. Neovim adds `lua/` to the Lua
`package.path` automatically, so `require("config.terminal")` maps to
`lua/config/terminal.lua`.

=== `lua/options.lua`

Sets `vim.opt.++*++` values (line numbers, tabs, scroll offset, etc.)
and the two leader keys:

[source,lua]
----
vim.g.mapleader      = " "   -- Space  (global keymaps)
vim.g.maplocalleader = ","   -- Comma  (filetype keymaps)
----

This file is loaded first so leader keys are defined before any plugin
or keymap tries to reference them.

=== `lua/keymaps.lua`

Global `++<++leader++>++` keymaps that are not tied to any plugin or
filetype. Things like window navigation, buffer management, and
diagnostic shortcuts.

=== `lua/loader/init.lua`

Bootstraps https://github.com/folke/lazy.nvim[lazy.nvim] (downloads it
if missing), then calls `lazy.setup()` with
`++{++ import = "plugins" }`. This tells lazy.nvim to auto-import every
file under `lua/plugins/` as a plugin spec.

=== `lua/plugins/` — Plugin Specs

One file per plugin group. Each file returns a lazy.nvim spec table.
Examples:

[width="100%",cols="50%,50%",options="header",]
|===
|File |What it configures
|`init.lua` |Bare-string plugins needing no config (statusline,
surround, etc.)

|`colorscheme.lua` |TokyoNight theme; change `style =` to switch
variants

|`avante.lua` |avante.nvim AI chat (Ollama-only backend)

|`conform.lua` |Format-on-save via conform.nvim

|`fzf-lua.lua` |Fuzzy finder (files, grep, buffers)

|`git.lua` |Fugitive {plus} Gitsigns

|`haskell.lua` |haskell-tools.nvim (LSP {plus} GHCi REPL)

|`lisp.lua` |Conjure, vim-sexp, parinfer, rainbow-delimiters (all Lisp
dialects)

|`markdown.lua` |markdown-preview.nvim (GUI) {plus} glow.nvim (console
terminal)

|`blink.lua` |blink.cmp completion engine (v1); blink.compat bridges for cmp-spell and cmp-conjure

|`nvim-tree.lua` |File explorer sidebar

|`plantuml.lua` |PlantUML syntax {plus} ASCII/PNG preview via Docker
server

|`treesitter.lua` |Tree-sitter parsers for syntax highlighting and indent. Pins `nvim-treesitter` to `master` and routes `opts` through `nvim-treesitter.configs.setup`. Markdown highlight/indent disabled (nil-range hotfix).
|===

Plugins use `ft = ++{++ "markdown" }` (lazy-load on filetype) or
`event = "VeryLazy"` (load after startup) to keep startup time fast.

=== `lua/config/` — Non-Plugin Configuration Modules

Reusable Lua modules that are `require()`d by plugin configs or
ftplugins. They do not return lazy.nvim specs.

[width="100%",cols="50%,50%",options="header",]
|===
|File |Purpose
|`terminal.lua` |Detects the terminal environment: `is++_++console`,
`is++_++wsl`, `has++_++nerd++_++font`, `has++_++undercurl`. Plugin
configs branch on these flags instead of hardcoding terminal-specific
behaviour.

|`lsp.lua` |Shared `on++_++attach` function and LSP server setup
(`lspconfig.++<++server++>++.setup++{++}`). All servers use the same
on++_++attach so keymaps are consistent.

|`treesitter.lua` |Tree-sitter parser list and configuration (called
from `lua/plugins/treesitter.lua`).

|`util.lua` |Small shared helpers, e.g. `open++_++url()` which respects
`is++_++console`.

|`confluence.lua` |`:MdToConfluence`, `:MdFromConfluence`,
`:MdConfluenceComments` commands. Requires `CONFLUENCE++_++EMAIL` {plus}
`CONFLUENCE++_++API++_++TOKEN` env vars.

|`jira.lua` |`:JiraCreateIssue`, `:JiraCreateStory` commands. Requires
`JIRA++_++EMAIL` {plus} `JIRA++_++API++_++TOKEN` {plus}
`JIRA++_++BASE++_++URL` env vars.

|`marp.lua` |`:MarpPreview`, `:MarpToPptx`, `:MarpToHtml`, `:MarpToPdf`
via the MARP Docker container.

|`mdpdf.lua` |`:MdToPdf` — Markdown → PDF with PlantUML diagrams via
Docker.

|`mdpreview.lua` |`:MdServerPreview` — live Markdown preview via the
markserv Docker container (handles cross-page links).

|`openspec.lua` |OpenSpec workflow integration (proposal/spec/task
management).

|`claude++_++cli.lua` |Helpers for the Claude CLI integration
(`:ClaudeSuggest` / `:ClaudeExplain`).
|===

'''''

[[after-ftplugin]]
== `after/ftplugin/` — Filetype-Specific Overrides

Neovim automatically sources `after/ftplugin/++<++filetype++>++.lua`
when a buffer of that filetype is opened. This is the correct place for:

* Setting `vim.b.maplocalleader`
* Filetype-specific indent settings
* `++<++localleader++>++` keymaps (REPL, preview, export, etc.)
* Calling `require("config.++<++module++>++").setup()` for filetype
modules

[width="100%",cols="34%,33%,33%",options="header",]
|===
|File |Filetype |Key things it does
|`markdown.lua` |`markdown` |`,p` preview (glow/markdown-preview);
Confluence, Jira, MARP, PDF commands

|`plantuml.lua` |`plantuml` |`,p` / `++<++leader++>++pa` diagram preview
via Docker

|`lisp.lua` |`lisp` |Conjure REPL keymaps, parinfer mode

|`clojure.lua` |`clojure` |Conjure REPL keymaps

|`scheme.lua` |`scheme` |Conjure REPL keymaps

|`fsharp.lua` |`fsharp` |iron.nvim REPL keymaps (`dotnet fsi`)

|`haskell.lua` |`haskell` |haskell-tools keymaps (GHCi REPL, Hoogle
search)

|`html.lua` |`html` |Indent overrides

|`http.lua` |`http` |rest.nvim request execution keymaps

|`cs.lua` |`cs` (C++#++) |.NET / OmniSharp keymaps
|===

'''''

[[docker]]
== `docker/` — Docker Service Definitions

Each subdirectory is a self-contained Docker Compose service used by a
Neovim feature. Start them independently with
`docker compose -f ++<++path++>++ up -d`.

[width="100%",cols="34%,33%,33%",options="header",]
|===
|Directory |Service |Used by
|`plantuml-server/` |PlantUML HTTP server (port 8080) |`:PumlPreview`,
`:PumlPreviewAscii`, Markdown PDF export

|`markserv/` |Markserv live-reload server |`:MdServerPreview`
(cross-page Markdown links)

|`marp/` |MARP CLI renderer |`:MarpPreview`, `:MarpToPptx`,
`:MarpToHtml`, `:MarpToPdf`

|`md2pdf/` |Pandoc {plus} filters for PDF export |`:MdToPdf` (includes
PlantUML {plus} Mermaid diagram rendering)

|`ollama/` |Ollama LLM server (port 11434) |avante.nvim ollama backend

|`sbcl-swank/` |SBCL Common Lisp {plus} Swank server (port 4005)
|Conjure Common Lisp REPL
|===

'''''

[[docs]]
== `docs/` — User Documentation

Human-readable reference material, authored in AsciiDoc. Not loaded by Neovim.

All documentation lives under `docs/modules/ROOT/pages/` as `.adoc` files —
this is the source of truth. There are no auto-generated files and no Markdown
source files under `docs/`.

=== `docs/guides/`

Long-form setup and usage guides, one per feature area. Start here when
setting up a new language or feature on a fresh machine.

=== `docs/cheatsheets/`

Quick-reference keymap tables. `index.adoc` is the single-page master
reference. Each other file covers one plugin or language area.

=== `docs/learning/`

Step-by-step learning paths for specific languages (currently Janet).

=== `docs/modules/ROOT/nav.adoc`

Sidebar navigation for the Antora docs site. Must be updated whenever
a page is added or removed. See xref:other/contributing.adoc[Contributing to Docs].

=== `jira-project-map.adoc`

Maps directory prefixes to Jira project keys. Used by
`lua/config/jira.lua` to automatically select the right Jira project
when creating issues.

'''''

[[scripts]]
== `scripts/` — Shell and Pandoc Scripts

Helper scripts invoked by Neovim commands, not intended to be run
directly by the user.

[width="100%",cols="50%,50%",options="header",]
|===
|File |Purpose
|`confluence++_++publish.sh` |Converts Markdown → Confluence storage
format and POSTs via REST API

|`confluence++_++preproc.py` |Python pre-processor: resolves local image
paths and PlantUML blocks before publish

|`confluence++_++filter.lua` |Pandoc Lua filter used during Confluence
conversion
|===

'''''

[[openspec]]
== `openspec/` — OpenSpec Change Management

OpenSpec is a lightweight spec-driven workflow for tracking proposed
changes to this configuration. Its workflow steps are available to
Claude Code as skills in `.claude/skills/` (e.g. `/openspec-propose`).

....
openspec/
  config.yaml          # Schema declaration (spec-driven)
  specs/               # Ratified/merged specs (source of truth)
  changes/             # In-progress changes
    <change-name>/
      proposal.md      # What and why
      design.md        # How (technical approach)
      tasks.md         # Implementation checklist
      specs/           # Delta specs for this change
        <feature>/
          spec.md      # Requirement + scenarios for one feature slice
    archive/           # Completed changes (historical record)
....

Active changes in `changes/` represent work in progress. Once
implemented and verified, a change is archived (moved to
`changes/archive/`) and its specs are merged into the top-level `specs/`
directory.

'''''

[[github]]
== `.github/` — GitHub Actions

[width="100%",cols="50%,50%",options="header",]
|===
|Path |Purpose
|`workflows/docs.yml` |CI workflow that builds and publishes the Antora
documentation site.
|===

'''''

[[claude]]
== `.claude/` — Claude Code Integration

[width="100%",cols="50%,50%",options="header",]
|===
|Path |Purpose
|`skills/` |Claude Code skill definitions — each `SKILL.md` is invocable
as a slash command (`/openspec-propose`, `/add-neovim-feature`, etc.).
Project guidance lives in `CLAUDE.md` at the repo root.

|`settings.local.json` |Project-local Claude Code settings (e.g.
permission allowlist).
|===

'''''

[[serena]]
== `.serena/` — Serena MCP Server State

Runtime state for the https://github.com/oraios/serena[Serena] MCP
server:

[width="100%",cols="50%,50%",options="header",]
|===
|Path |Purpose
|`project.yml` |Project-level Serena configuration (shared, committed)

|`project.local.yml` |Machine-local overrides (gitignored)

|`cache/` |Serena’s symbol index cache (gitignored, rebuilt
automatically)

|`memories/` |Persistent memory files written by AI assistants during
sessions
|===

'''''

[[testdocs]]
== `testdocs/`

Sample files used for manual testing of preview features
(e.g. `test.puml` for verifying the PlantUML ASCII preview). Not part of
the production config.

'''''

[[console-detection]]
== Console Detection

The configuration detects console mode via a single boolean flag:

[source,lua]
----
local term = require("config.terminal")
if term.is_console then ...
----

`is++_++console` is `true` when *both* `$DISPLAY` and
`$WAYLAND++_++DISPLAY` are unset. It is `false` when either is set,
which covers:

[width="100%",cols="25%,25%,25%,25%",options="header",]
|===
|Environment |`$DISPLAY` |`$WAYLAND++_++DISPLAY` |`is++_++console`
|Physical TTY (`$TERM=linux`) |unset |unset |`true`
|SSH without X forwarding |unset |unset |`true`
|tmux on headless server |unset |unset |`true`
|X11 GUI terminal |set (e.g. `:0`) |unset |`false`
|Wayland GUI terminal |unset |set (e.g. `wayland-0`) |`false`
|tmux inside GUI terminal |set (outer terminal sets it) |— |`false`
|===

No manual override flag is needed. If the detection is wrong for your
setup, file an issue.

'''''

[[open-url]]
== Open URL Behaviour

In console mode, `util.open++_++url` skips all browser-opener attempts
and instead emits an `INFO` notification containing the full URL. Copy
it and open it manually (e.g. in w3m, or paste it into a GUI browser on
another machine).

In GUI mode, the existing behaviour is unchanged: `xdg-open` / `open` /
`wslview` / `explorer.exe` are tried in order, and a `WARN` notification
is emitted if none are found.
