== Git Cheatsheet

*Leader* = `Space`

→ Back to xref:editor/keybindings.adoc[main cheatsheet]

'''''

== vim-fugitive — Git Operations

Run any git command with `:Git ++<++args++>++` or its short alias
`:G ++<++args++>++` (e.g. `:Git commit`, `:G rebase -i HEAD~3`).

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++<++leader++>++gs` |Normal |Open git status (`:Git`)
|`++<++leader++>++gb` |Normal |Git blame for current file
|`++<++leader++>++gl` |Normal |Git log
|`++<++leader++>++gd` |Normal |Git diff (unstaged changes)
|`++<++leader++>++gp` |Normal |Git push
|===

=== Inside the `:Git` status window — Staging

[cols=",",options="header",]
|===
|Keys |Action
|`s` |Stage file / hunk under cursor
|`u` |Unstage file / hunk under cursor
|`-` |Toggle stage / unstage
|`U` |Unstage everything
|`X` |Discard change under cursor
|`=` |Toggle inline diff
|`++>++` / `++<++` |Show / hide inline diff
|`I` |Interactive patch (`:Git add --patch` / `reset --patch`)
|===

=== Inside the `:Git` status window — Commit

[cols=",",options="header",]
|===
|Keys |Action
|`cc` |Create a commit
|`ca` |Amend last commit (edit message)
|`ce` |Amend last commit (keep message)
|`cw` |Reword last commit
|`cf` |Create `fixup!` commit for commit under cursor
|`crc` |Revert commit under cursor
|===

=== Inside the `:Git` status window — Navigation & Diff

[cols=",",options="header",]
|===
|Keys |Action
|`(` / `)` |Previous / next file or hunk
|`++[++c` / `++]++c` |Previous / next hunk (auto-expands inline diffs)
|`++[++[` / `++]++]` |Jump section backward / forward
|`gu` |Jump to Untracked section
|`gs` |Jump to Staged section
|`dd` |`:Gdiffsplit` on file under cursor
|`dv` |`:Gvdiffsplit` (vertical)
|`ds` |`:Ghdiffsplit` (horizontal)
|`++<++CR++>++` |Open file under cursor
|`o` |Open in horizontal split
|`gO` |Open in vertical split
|`O` |Open in new tab
|`gq` |Close status buffer
|`g?` |Help — all fugitive maps
|===

=== Inside the `:Git` status window — Stash

[cols=",",options="header",]
|===
|Keys |Action
|`czz` |Push stash
|`czw` |Push stash of working tree (`--keep-index`)
|`czs` |Push stash of stage only
|`czp` / `czP` |Pop / apply topmost stash
|===

'''''

== gitsigns.nvim — In-Buffer Git Signs

Changed lines are marked in the sign column: `▎` added · `▎` changed ·
`▁`/`▔` deleted line

=== Hunk Navigation

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++]++h` |Normal |Jump to next hunk
|`++[++h` |Normal |Jump to previous hunk
|===

=== Hunk Actions

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++<++leader++>++hs` |Normal / Visual |Stage hunk (or selected lines)
|`++<++leader++>++hr` |Normal / Visual |Reset hunk (or selected lines)
|`++<++leader++>++hS` |Normal |Stage entire buffer
|`++<++leader++>++hR` |Normal |Reset entire buffer
|`++<++leader++>++hu` |Normal |Undo last stage
|`++<++leader++>++hp` |Normal |Preview hunk inline
|`++<++leader++>++hb` |Normal |Full blame for current line
|`++<++leader++>++hd` |Normal |Diff this file (unstaged)
|`++<++leader++>++hD` |Normal |Diff this file (staged, vs. HEAD)
|===

=== Text Object

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++<++leader++>++ih` |Visual / Operator |Select current hunk
|===

'''''

____
*Note — Haskell buffers:* `++<++leader++>++hs` is used by haskell-tools
for Hoogle search in `.hs` files. The buffer-local haskell binding takes
precedence there; use the fugitive status window (`:Git`) to stage
changes while editing Haskell.
____

'''''

== diffview.nvim — Side-by-Side Diff & File History

Opens a persistent tabbed diff panel with a file list on the left and a
two-pane diff on the right. Also provides per-file commit history and a
three-way merge conflict view.

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++<++leader++>++gD` |Normal |Open diff view (all working tree changes)
|`++<++leader++>++gH` |Normal |Open file history for current file
|`++<++leader++>++gX` |Normal |Close diff view
|===

You can also open diffview against any ref directly:

* `:DiffviewOpen HEAD~3` — diff working tree vs 3 commits ago
* `:DiffviewFileHistory` — history for all files (omit `%` for repo-wide)

'''''

_Plugin configured in `lua/plugins/git.lua`._
