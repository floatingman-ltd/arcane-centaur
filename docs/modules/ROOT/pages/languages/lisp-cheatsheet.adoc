== Lisp Cheatsheet (Conjure {plus} vim-sexp)

*LocalLeader* = `,` in Lisp / Clojure / Scheme / Fennel buffers

→ Back to xref:editor/keybindings.adoc[main cheatsheet] · Full guide: ../guides/lisp.md
· Janet-specific: janet.md

== Common Lisp REPL Containers

Compose file: `docker/lisp-swank/docker-compose.yml`

*The :4005 contract:* whoever owns `127.0.0.1:4005` is the active Lisp.
Switch by bringing down the current profile and bringing up the next; reconnect with `,cc`.

[cols="1,3",options="header"]
|===
|Implementation |Start command
|SBCL (default)  |`LISP_DIR=$PWD docker compose -f ~/.config/nvim/docker/lisp-swank/docker-compose.yml up -d --wait`
|CCL  |`LISP_DIR=$PWD docker compose -f ~/.config/nvim/docker/lisp-swank/docker-compose.yml --profile ccl  up -d --wait`
|ECL  |`LISP_DIR=$PWD docker compose -f ~/.config/nvim/docker/lisp-swank/docker-compose.yml --profile ecl  up -d --wait`
|ABCL |`LISP_DIR=$PWD docker compose -f ~/.config/nvim/docker/lisp-swank/docker-compose.yml --profile abcl up -d --wait`
|===

[source,sh]
----
# Switch: bring down current, bring up next, reconnect
docker compose -f ~/.config/nvim/docker/lisp-swank/docker-compose.yml down
# ...then the start command for the target impl above
# Then in Neovim: ,cc
----

'''''

== Conjure — Evaluation (all clients)

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`,ee` |Normal |Evaluate form under cursor
|`,er` |Normal |Evaluate root (outermost) form
|`,em` |Normal |Evaluate marked form
|`,ew` |Normal |Evaluate word under cursor
|`,ep` |Normal |Evaluate and display result inline
|`,e!` |Normal |Evaluate and replace with result
|`,eb` |Normal |Evaluate buffer (from memory)
|`,ef` |Normal |Evaluate file (load from disk)
|`,ece` |Normal |Evaluate form, insert result as comment
|`,ecr` |Normal |Evaluate root form, insert result as comment
|`,E` |Visual |Evaluate visual selection
|`,E` |Normal (operator) |Evaluate over motion
|`,gd` |Normal |Go to definition
|`K` |Normal |Look up documentation (bare key — no leader prefix)
|===

== Conjure — Log Window

[cols=",",options="header",]
|===
|Keys |Action
|`,ls` |Open log in horizontal split
|`,lv` |Open log in vertical split
|`,lt` |Open log in new tab
|`,le` |Open log in current window
|`,lg` |Toggle log window open / closed
|`,lr` |Reset log — soft (wipe contents)
|`,lR` |Reset log — hard (delete buffer)
|`,ll` |Jump to latest result in log
|`,lq` |Close all visible log windows
|===

== Conjure — Connection (Common Lisp / Swank)

[cols=",",options="header",]
|===
|Keys |Action
|`,cc` |Connect to Swank REPL (port 4005)
|`,cd` |Disconnect from REPL
|===

== Conjure — Clojure Extras (nREPL)

[cols=",",options="header",]
|===
|Keys |Action
|`,cf` |Connect via `.nrepl-port` file
|`,ei` |Interrupt current evaluation
|`,ve` |Display last exception
|`,v1` / `,v2` / `,v3` |Display recent results (1st / 2nd / 3rd)
|`,vs` |View source of symbol under cursor
|`,ta` |Run all tests
|`,tn` |Run tests in current namespace
|`,tc` |Run test under cursor
|`,rr` |Refresh changed namespaces
|`,ra` |Refresh all namespaces
|===

== Structural Editing — vim-sexp-mappings-for-regular-people

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++>++)` |Normal |*Slurp forward* — extend `)` rightward, pulling next sibling _into_ the form
|`++<++)` |Normal |*Barf forward* — shrink `)` leftward, pushing last element _out_
|`++<++(` |Normal |*Slurp backward* — extend `(` leftward, pulling previous sibling _in_
|`++>++(` |Normal |*Barf backward* — shrink `(` rightward, pushing first element _out_
|`++>++e` |Normal |Swap element forward
|`++<++e` |Normal |Swap element backward
|`++>++f` |Normal |Swap form forward
|`++<++f` |Normal |Swap form backward
|`++<++I` |Normal |Insert at head of current form
|`++>++I` |Normal |Insert at tail of current form
|`dsf` |Normal |Splice — delete surrounding brackets
|`cse(` or `cse)` |Normal / Visual |Wrap element in `()`
|`cse++[++` or `cse++]++` |Normal / Visual |Wrap element in `++[]++`
|`cse{` or `cse}` |Normal / Visual |Wrap element in `{}`
|`W` / `B` / `E` |Normal / Visual / Operator |Next/prev element head; next element tail
|===

== vim-sexp — Text Objects

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`af` / `if` |Visual / Operator |Outer / inner list (compound form), incl./excl. brackets
|`aF` / `iF` |Visual / Operator |Outer / inner top-level list
|`as` / `is` |Visual / Operator |Outer / inner string (incl./excl. quotes)
|`ae` / `ie` |Visual / Operator |Outer / inner element (atom/string/comment/form)
|===

== vim-sexp — Motions

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`(` / `)` |Normal / Visual / Operator |Move to paired brackets backward / forward
|`++[++[` / `++]++]` |Normal / Visual / Operator |Previous / next top-level element
|`++[++e` / `++]++e` |Normal / Visual / Operator |Select previous / next element
|===

____
See
xref:languages/lisp.adoc#structural-editing[../guides/lisp.md]
for step-by-step examples of each slurp/barf operation.
____

== Other Active Plugins

* *nvim-parinfer* — keeps parentheses balanced as you edit indentation
(no keymaps)
* *rainbow-delimiters.nvim* — color-codes matching delimiters by depth
(no keymaps)
* *LSP* (none configured by default for Common Lisp) — see lsp.md
* *Formatting* (conform.nvim, format-on-save) — see formatting.md

'''''

_Conjure prefix configured in `lua/plugins/lisp.lua`. vim-sexp keymaps
from `tpope/vim-sexp-mappings-for-regular-people`. LocalLeader set in
`after/ftplugin/lisp.lua`, `clojure.lua`, and `scheme.lua`._
