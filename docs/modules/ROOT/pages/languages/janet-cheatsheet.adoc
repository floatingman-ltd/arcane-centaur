== Janet Cheatsheet (Conjure {plus} vim-sexp)

*LocalLeader* = `,` in Janet buffers

→ Back to xref:editor/keybindings.adoc[main cheatsheet] · xref:languages/janet.adoc[Full guide]

NOTE: This config uses the Janet *stdio client* (`janet -n -s`). Conjure spawns it automatically on first evaluation; use `,cs`/`,cS` to restart it manually.

== Conjure — Evaluation

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`,ee` |Normal |Evaluate form under cursor
|`,er` |Normal |Evaluate root (outermost) form
|`,em` |Normal |Evaluate marked form
|`,ew` |Normal |Evaluate word under cursor
|`,ep` |Normal |Evaluate and display result inline
|`,e!` |Normal |Evaluate and replace with result
|`,eb` |Normal |Evaluate buffer (from memory)
|`,ef` |Normal |Evaluate file (load from disk)
|`,E` |Visual |Evaluate visual selection
|`,gd` |Normal |Go to definition
|`K` |Normal |Look up documentation (bare key — no leader prefix)
|===

== Conjure — Log Window

[cols=",",options="header",]
|===
|Keys |Action
|`,ls` |Open log in horizontal split
|`,lv` |Open log in vertical split
|`,lt` |Open log in new tab
|`,lg` |Toggle log window open / closed
|`,ll` |Jump to latest result in log
|`,lq` |Close all visible log windows
|===

== Conjure — Connection (Janet stdio)

[cols=",",options="header",]
|===
|Keys |Action
|`,cs` |Start the Janet REPL subprocess
|`,cS` |Stop the Janet REPL subprocess
|===

== Structural Editing — vim-sexp-mappings-for-regular-people

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`++>++)` |Normal |*Slurp forward* — extend `)` rightward, pulling next sibling _into_ the form
|`++<++)` |Normal |*Barf forward* — shrink `)` leftward, pushing last element _out_
|`++<++(` |Normal |*Slurp backward* — extend `(` leftward
|`++>++(` |Normal |*Barf backward* — shrink `(` rightward
|`++>++e` / `++<++e` |Normal |Swap element forward / backward
|`++>++f` / `++<++f` |Normal |Swap form forward / backward
|`++<++I` / `++>++I` |Normal |Insert at head / tail of current form
|`dsf` |Normal |Splice — delete surrounding brackets
|`cse(` or `cse)` |Normal / Visual |Wrap element in `()`
|`cse++[++` or `cse++]++` |Normal / Visual |Wrap element in `++[]++`
|`cse{` or `cse}` |Normal / Visual |Wrap element in `{}`
|===

== vim-sexp — Text Objects

[cols=",,",options="header",]
|===
|Keys |Mode |Action
|`af` / `if` |Visual / Operator |Outer / inner list (compound form)
|`as` / `is` |Visual / Operator |Outer / inner string
|`ae` / `ie` |Visual / Operator |Outer / inner element
|===

== Other Active Plugins

* *nvim-parinfer* — keeps parentheses balanced as you edit indentation
(no keymaps)
* *rainbow-delimiters.nvim* — color-codes matching delimiters by depth
(no keymaps)
* *LSP* (`janet++_++lsp`) — see lsp.md
* *Formatting* (conform.nvim, format-on-save) — see formatting.md

'''''

_Conjure prefix configured in `lua/plugins/lisp.lua`. vim-sexp keymaps
from `tpope/vim-sexp-mappings-for-regular-people`. LocalLeader set in
`after/ftplugin/janet.lua`._
