# add-research-popup

A lightweight in-editor "research popup": type a free-form question, get a prose answer in a floating window. Two entry points with identical look and feel — `<leader>?l` (grounded in this config) and `<leader>?a` (general knowledge) — differing only in the source of the answer.
